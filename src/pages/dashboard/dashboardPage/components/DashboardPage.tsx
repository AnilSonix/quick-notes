import AuthProvider from "../../authProvider/components/AuthProvider";

const DashboardPage = () => {
  return (
    <AuthProvider>
      <div>dash</div>
    </AuthProvider>
  );
};

export default DashboardPage;
